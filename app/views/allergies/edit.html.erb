<% provide(:title, 'Edit Allergy Record') %>
<div class='row'>
	<%= render 'shared/phr_info' %>

	<div class="span8">
	      <h2 style="text-align: left;">Edit Allergy Record</h2>
	      <hr>
	      <%= form_for([@phr, @allergy]) do |f| %>
	        <%= render 'shared/error_messages', object: f.object %>
	        <div class="span6">
	          <%= render 'allergy_form', f: f %>
	        </div>
		<% end %>
	</div>	
</div>

