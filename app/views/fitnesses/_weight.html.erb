		<table class = "table table-striped table-bordered table-condensed">
								<thead>
									<tr>
										<th style = "width: 50%">Date</th>
										<th style = "width: 50%">Weight</th>
									</tr>
								</thead>

								<tbody>
									<% result = @fitnesses.select("date, weight").where(deleted: false).where.not(height: nil).order(date: :desc).first() %>
									<td><%= result.date.strftime('%m/%d/%Y') %></td>
									<td><%= result.weight %></td>
								</tbody>
								</table>