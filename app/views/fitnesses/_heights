<% unless fitness.deleted %>
	<tr>
		<td><%= fitness.date.strftime('%m/%d/%Y') %></td>
		<td><%= fitness.height %></td>
		<td><%= fitness.weight %></td>
		
		<td><%= link_to 'Delete', [fitness.phr, fitness], method: :delete, data: {confirm: "Are you sure you want to delete this fitness record?"}, title: fitness.date, class: "btn btn-small btn-secondary" %></td>
	</tr>
<% end %>