								<table class = "table table-striped table-bordered table-condensed">
								<thead>
									<tr>
										<th style = "width: 50%">Date</th>
										<th style = "width: 50%">Height</th>
									</tr>
								</thead>

								<tbody>
									<% result = @fitnesses.select("date, height").where(deleted: false).where.not(height: nil).order(date: :desc).first() %>
									<td><%= result.date.strftime('%m/%d/%Y') %></td>
									<td><%= result.height %></td>
								</tbody>

								</table>	