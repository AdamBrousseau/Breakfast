<% provide(:title, 'Edit Optometrist Record') %>

<%= form_for([@phr, @eye]) do |f| %>
<%= render 'shared/phr_info' %>
  <div class="row">
      <h1>Update Optometrist Record</h1>
      <%= render 'shared/error_messages', object: f.object %>
     
        <div class="span6">
          <%= render 'eye_form', f: f %>
        </div>


  </div>
<% end %>
</div>