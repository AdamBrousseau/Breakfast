<li>
	  <span class="first_name"><%= user.name %></span>
	  <span class="timestamp">
	    Created <%= user.created_at.strftime('%B %-d, %Y') %>.
	    Last updated <%= time_ago_in_words(user.updated_at) %> ago.
	    <%= link_to "Delete", user, method: :delete, data: { confirm: "Are you sure you want to delete this User and all their PHRs and Medical Records?" } , title: user.name%>
	  </span>
</li>