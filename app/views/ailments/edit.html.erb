<% provide(:title, "Edit Ailment Record") %>
<div class='row'>
	<%= render 'shared/phr_info' %>
	
	<div class="span8">
		<h2 style = "text-align: left;">Edit Ailment Record</h2>
		<hr>


		<%= form_for([@phr, @ailment]) do |f| %>
		<%= render 'shared/error_messages', object: f.object %>
		
		<div class="span6">
			<%= render 'form', f: f %>
		</div>
		<% end %>
	</div>
</div>