	<p>
		<%= f.label :begin_date %>
		<%= f.date_select :begin_date, :start_year => 1900, :end_year => Time.now.year %>
	</p>

	<p>
		<%= f.label :end_date %>
		<%= f.date_select :end_date, :start_year => 1900, :end_year => Time.now.year, :include_blank => true, :default => nil %>
	</p>

	<p>
		<%= f.label :ailment_name %>
		<%= f.text_field :ailment_name %>
	</p>		
	
	<p>
		<%= f.label :ailment_description %>
		<%= f.text_area :ailment_description %>
	</p>

	<p>
		<%= f.label :comments %>
		<%= f.text_area :comments %>
	</p>

	<p>
	<%= f.submit "Submit", :class => "btn btn-small btn-secondary" %> <%= link_to 'Cancel', phr_ailments_path, class: "btn btn-small btn-secondary" %>

	</p>
